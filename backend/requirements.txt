# =============================================================================
# Survey Chatbot — Backend Dependencies
# Install with: pip install -r requirements.txt
# =============================================================================

# Web framework
fastapi==0.110.0
uvicorn[standard]==0.29.0
aiofiles==23.2.1          # Required for FastAPI StaticFiles (serving frontend)

# Environment variables
python-dotenv==1.0.1

# Data
pandas==2.2.2
numpy==1.26.4
tabulate==0.9.0

# Google Sheets access (API Key method — no service account JSON needed)
requests==2.32.3

# LangChain core
langchain==0.2.6
langchain-core==0.2.10

# LangChain Pandas DataFrame Agent (experimental)
langchain-experimental==0.0.62

# OpenRouter (OpenAI-compatible) LLM integration
langchain-openai==0.1.25
