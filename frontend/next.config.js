/** @type {import('next').NextConfig} */
const nextConfig = {
  // For Vercel: no output override needed — Vercel handles Next.js natively.
  // For local dev (FastAPI serves static files): uncomment the two lines below:
  //   output: "export",
  //   trailingSlash: true,
};

module.exports = nextConfig;
